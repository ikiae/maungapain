/*******************************************************************************

    Title :  
    Date  :  July 2014
    Author:  Suitmedia (http://www.suitmedia.com)

********************************************************************************/

var Site = {

    assets: {
        // _plugin: myPrefix + [plugin path]
    },

    init: function () {
        Site.fastClick();
        Site.enableActiveStateMobile();
        Site.WPViewportFix();
    },

    fastClick: function () {
        FastClick.attach(document.body);
    },

    enableActiveStateMobile: function () {
        if ( document.addEventListener ) {
            document.addEventListener('touchstart', function () {}, true);
        }
    },

    WPViewportFix: function () {
        if ( navigator.userAgent.match(/IEMobile\/10\.0/) ) {
            var style   = document.createElement("style"),
                fix     = document.createTextNode("@-ms-viewport{width:auto!important}");

            style.appendChild(fix);
            document.getElementsByTagName('head')[0].appendChild(style);
        }
    }

};

$(document).ready(function(){
    Site.init();

    $(".bxslider").bxSlider();

    // $(".content__main__tab__btn").click(function(){
    //     var contoh = $(this).attr("data-id"); 
    //     var target = $('#' + contoh); 
    //     $(".content__main__main1").hide(); 
    //     target.show(); 
    //     $(".content__main__tab__btn").removeClass("content__main__tab__btn--active");
    //     $(this).addClass("content__main__tab__btn--active");
    // });

    // 

    // // settings
    // var $slider = $('.slider__image'); // class or id of carousel slider
    // var $slide = 'li'; // could also use 'img' if you're not using a ul
    // var $transition_time = 1000; // 1 second
    // var $time_between_slides = 4000; // 4 seconds

    // function slides(){
    //    return $slider.find($slide);
    // }

    // slides().fadeOut();

    // // set active classes
    // slides().first().addClass('active');
    // // slide().animate({
    // //     'margin-left' : += "30px"
    // // });
    // slides().first().fadeIn($transition_time);

    // // auto scroll 
    // $interval = setInterval(
    //     function(){
    //         var $i = $slider.find($slide + '.active').index();

    //         slides().eq($i).removeClass('active');
    //         slides().eq($i).fadeOut($transition_time);

    //         if (slides().length == $i + 1) $i = -1; // loop to start

    //         slides().eq($i + 1).fadeIn($transition_time);
    //         slides().eq($i + 1).addClass('active');
    //     }
    //     , $transition_time +  $time_between_slides 
    //   );
});  